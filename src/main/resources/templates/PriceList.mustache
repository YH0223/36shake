<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ckt_post_2_html</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Inter', sans-serif;
            display: flex;
            justify-content: center;
        }
        .Frame1 {
            width: 100%;
            max-width: 375px;
            background: white;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .Frame1 img {
            width: 100%;
            height: auto;
        }
        .Frame1 .Frame2, .Frame1 .Frame3 {
            width: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
            border-top: 1px solid #ccc;
        }
        .Frame1 .Frame2 > div, .Frame1 .Frame3 > div {
            width: 90%;
            max-width: 375px;
            padding: 10px;
            margin: 5px 0;
            color: black;
            font-size: 15px;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-shadow: 0 1px 2px rgba(0, 0, 0, 0.1);
            background: #fff;
            word-wrap: break-word;
        }
        .Postid {
            height: auto;
            text-align: center;
        }
        /* 로딩 표시기 컨테이너 스타일 */
        .loading-spinner-container {
            display: none; /* 기본적으로 숨김 */
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(255, 255, 255, 0.7); /* 반투명한 흰색 배경 */
            z-index: 9999; /* 다른 요소 위에 표시 */
        }

        /* 로딩 표시기 스타일 */
        .loading-spinner {
            border: 4px solid rgba(0, 0, 0, 0.1);
            border-left: 4px solid #333;
            border-radius: 50%;
            width: 50px;
            height: 50px;
            animation: spin 1s linear infinite;
            position: absolute;
            top: 50%;
            left: 50%;
            margin-top: -25px;
            margin-left: -25px;
        }

        /* 스핀 애니메이션 */
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
    </style>
</head>
<body>
<!-- 로딩 표시기 -->
<div class="loading-spinner-container">
    <div class="loading-spinner">
        사이트별 최저가를 불러오고있어요!
        조금만 기다려주세요!
    </div>
</div>
<div class="Frame1">
    <div class="Frame2">
        <div class="Ckt_name">
            <img src="/imageig?igid={{igid}}">
            {{name}}</div>
    </div>
    <div class="Frame3">
        {{#siteResults}}
            <div class="site-result">
                <div><strong>사이트:</strong> {{siteName}}</div>
                <div><strong>제품명:</strong> {{itemName}}</div>
                <div><strong>가격:</strong> {{price}}</div>
                <div><strong>링크:</strong> <a href="{{link}}" target="_blank"><div>구매링크</div></a></div>
            </div>
        {{/siteResults}}
    </div>
</div>
<script>
    document.addEventListener("DOMContentLoaded", function() {
        // 페이지가 로드되면 로딩 표시기를 표시합니다.
        showLoadingSpinner();
        window.addEventListener("load", function() {
            hideLoadingSpinner();
        });
    });

    // 로딩 표시기 표시 함수
    function showLoadingSpinner() {
        var spinnerContainer = document.querySelector(".loading-spinner-container");
        spinnerContainer.style.display = "block";
    }

    // 로딩 표시기 숨김 함수
    function hideLoadingSpinner() {
        var spinnerContainer = document.querySelector(".loading-spinner-container");
        spinnerContainer.style.display = "none";
    }
</script>
</body>
</html>